# Pinned oracle: the "throws only std::bad_alloc" contract membership
# population, independently derived by tests/ci/derive_bad_alloc_membership.py
# (S-HARDEN-7, design Sec3.1, T-411). One member per line: header:name.
# Regenerate with: python tests/ci/derive_bad_alloc_membership.py --oracle
# (SUPERSLM_CLANGXX="C:\Program Files\LLVM\bin\clang++.exe" if not on PATH),
# and paste its output verbatim below this comment block.
#
# T-2458 (Claude/Poirot/5c82f92-t2453-ask5-tracka-confirmation.md, Critical 1
# + Observation 2, D-SLM5528): the line column is DROPPED as of this change.
# Independently derived from `git log --follow` on this file rather than
# taken from the notes below (which undercount and, for T-2125, miscount):
# thirteen total commits touch this file end to end, `5043678` (creation, 18
# members) through this one. Of the twelve after creation, TWO are genuine
# membership growth -- `8ce6aa7` added `proof_manifest.h:JsonEscape`
# (eighteen to nineteen) and `242dc12` (T-2125) added a THIRD
# `model.h:Parse` (the population's only same-name-duplicate growth -- two
# Parse entries already existed and this made a third, taking that key's own
# count from two to three under the multiset; the population's 20th member
# overall, nineteen to twenty) -- and the other EIGHT are pure line shifts
# with the member set byte-for-byte unchanged: `480d610`/`d7d9442` (the
# initial S-HARDEN-7/8 build-out, before either growth), `b8ecff5` (S3.7,
# between the two growths), `70abbed` (T-2152), `2809d73` (the 2026-08-18
# banner scrub), `0ef481d` (the 2026-08-21 release, a pure line shift),
# `e3bc0cf` (T-2445), and `5c82f92`'s own regeneration (T-2458, regenerating
# after T-2450's comment insert). Not one of those eight pure-line-shift
# regenerations ever caught a membership change the name alone did not. The
# remaining TWO commits after those ten are comment-only corrections to THIS
# paragraph, touching neither the member set nor the line-count-derivation
# method: `6597903` (T-2467, correcting a THIRD-vs-FOURTH `model.h:Parse`
# miscount this paragraph itself carried) and this commit (T-2481, Claude/
# Poirot/6597903-t2472-ask5-tracka-confirmation.md Sec8 M1, D-SLM5611:
# correcting THIS sentence's own commit-count miscount -- it stated "eleven
# total / ten after creation" from the count as of `5c82f92`'s own
# regeneration and was never updated by the two comment-only corrections
# landed after it, so the commit that fixed the prior miscount immediately
# became a new instance of the same class it was fixing. A commit-count
# stated as a present-tense fact in a paragraph that later commits edit will
# recur; this correction states the count as of itself rather than pretend a
# static number can stay current -- read `git log --follow` on this file for
# the count as of any LATER commit).
#
# T-2467 (Claude/Poirot/665f430-t2462-ask5-tracka-confirmation.md, Minor 1,
# D-SLM5560): this paragraph used to state `242dc12` added a FOURTH
# `model.h:Parse`, self-contradicting the next paragraph's own "Parse appears
# three times below rather than once" four lines later. Executed against
# every commit's own blob: the key went two to three, not three to four. The
# conclusion this paragraph draws from the count -- that a plain SET would
# have absorbed the growth, so the multiset is the correct identity -- was
# unaffected and is independently re-confirmed by
# `test_vitality_injected_same_named_duplicate_is_flagged_by_multiset_not_by_set`
# in `test_membership_check_population.py`. Also corrected here (Minor 3,
# D-SLM5562): the `0ef481d` entry above used to cite "the prior note below's
# 'artifact declarations moved four members'" -- `566bf98` replaced the
# per-event notes this cited with the single summary paragraph above, which
# does not contain that phrase; the citation was dangling (its only match in
# this file was itself). Removed rather than repaired to point elsewhere,
# since `0ef481d` needs no further explanation than "a pure line shift."
#
# This oracle's identity is now the (header, name) MULTISET, not a set: a
# plain set would have silently absorbed T-2125's own third `Parse` as
# "already present" (two `model.h:Parse` entries already existed), which is
# why `Parse` appears three times below rather than once -- duplicates are
# repeated per member, never collapsed. Confirmed against the
# independently-derived history above: every one of the eight pure-line-shift
# commits is a byte-for-byte no-op under this format (re-verified by
# re-deriving each commit's own `header:line:name` triples and projecting
# both sides down to `(header, name)` counts); both real-growth commits still
# change it, including under a MULTISET rather than a SET comparison -- the
# distinction the T-2125 case exists to make. See
# `test_membership_check_population.py`'s `_load_pinned_oracle`/`_multiset`
# and `derive_bad_alloc_membership.py`'s `format_oracle_lines` for the
# comparison this file is read against.
#
# Prior history (line-column era, preserved for provenance): 20 sites as of
# T-2125 (D-SLM3446) -- SslmAmplifyingFoldScaleView<Kind>::Parse (model.h),
# the T-2021/T-2029 B0b runtime-additive-LoRA adapter view's own parse entry
# point, found unwrapped by this suite's own regression pin and closed by a
# rename-and-wrap fix in the same change (src/model.h/model.cpp). T-2152,
# 2026-08-18, 2026-08-21, and T-2445 (Claude/Poirot/ddbc57a-t2443-ask5-tracka-
# confirmation.md, Critical 1) each re-derived line numbers only, same
# 20-member set, names and files unchanged, per that era's own regeneration
# discipline (SUPERSLM_CLANGXX="C:\Program Files\LLVM\bin\clang++.exe", clang
# 18.1.8, the CI pin) -- superseded by this format, not restated per-member.
artifact.h:FingerprintHex
artifact.h:OpenFromFile
artifact.h:OpenFromMemory
model.h:Load
model.h:Parse
model.h:Parse
model.h:Parse
model.h:ParseConfig
model.h:ParseSigmoidLut
proof_manifest.h:BuildProofManifestJson
proof_manifest.h:ComputeTensorEvidence
proof_manifest.h:ComputeWeightScaleEvidence
proof_manifest.h:HashSectionHex
proof_manifest.h:JsonEscape
sha256.h:Sha256Hash
sha256.h:ToHex
sha256.h:Update
tokenizer.h:Decode
tokenizer.h:Encode
tokenizer.h:Open
