{
  "plan": "Claude/Plans/te266-gpu-path.md#3.10.8",
  "plan_commit": "669d2ce24e",
  "engine_red_pin": "fa04cf44c6dae4f24950e00d6ef9cd278da07b12",
  "fixture": {
    "schemas": ["t2922_accepts_q", "t2922_rejects_q"],
    "same_reachable_state": "q",
    "acceptance": {"t2922_accepts_q": 1, "t2922_rejects_q": 0}
  },
  "cells": [
    {"id":"gpu_accepting_api","kind":"new_api","oracle":"separate compile/link failure until SslmGpuSeqSchemaAcceptingForG5Bridge exists"},
    {"id":"gpu_bound_api","kind":"new_api","oracle":"separate compile/link failure until SslmGpuSeqSchemaBoundForG5Bridge exists"},
    {"id":"cpu_bound_api","kind":"new_api","oracle":"separate compile/link failure until sslm_seq_schema_bound exists"},
    {"id":"unbound_application_final","kind":"query","oracle":"accepting=0,bound=0"},
    {"id":"bound_mid_walk","kind":"query","oracle":"membership, not state!=0"},
    {"id":"bound_accepting","kind":"query","oracle":"membership=1"},
    {"id":"dead_end_predecessor","kind":"query","oracle":"failed finish preserves pre-dead-end membership"},
    {"id":"slm5_restore_bound","kind":"persistence","oracle":"bound=1 and membership preserved"},
    {"id":"slm4_restore_unbound","kind":"persistence","oracle":"accepting=0,bound=0"},
    {"id":"reset_preserves_binding","kind":"lifecycle","oracle":"bound=1,walk=0,membership from state zero"},
    {"id":"cpu_adopt_prefix_progress","kind":"composition","oracle":"walk and membership follow progressed prefix"},
    {"id":"cpu_adopt_prefix_prompt_only","kind":"composition","oracle":"binding retained, stale walk reset to zero"},
    {"id":"submitted_busy_unchanged_out","kind":"status","oracle":"GPU busy leaves both outputs unchanged"},
    {"id":"malformed_foreign_unchanged_out","kind":"status","oracle":"documented error leaves outputs unchanged"},
    {"id":"drain_then_finalize_query","kind":"status","oracle":"Idle query works before and after finish"},
    {"id":"distinct_schema_restore_rebind","kind":"identity","oracle":"same q reports opposite membership under A and B"},
    {"id":"gpu_late_live_bind_reject","kind":"existing_behavior","oracle":"generated unbound history rejects bind"},
    {"id":"gpu_late_slm5_bind_reject","kind":"existing_behavior","oracle":"restored generated unbound history rejects bind"},
    {"id":"fresh_reset_bind_accept","kind":"control","oracle":"fresh and reset sequences accept bind"},
    {"id":"cpu_terminal_reset_reuse","kind":"existing_behavior","oracle":"Idle terminal guard refusal resets and reuses"},
    {"id":"gpu_idle_partial_reset_reuse","kind":"existing_behavior","oracle":"all Idle strict partial layers reset and reuse"},
    {"id":"gpu_submitted_reset_busy","kind":"control","oracle":"Submitted alone returns busy"},
    {"id":"no_device_work","kind":"instrumentation","oracle":"both queries leave dispatch,ready,wait counters unchanged; commissioned decode increments"},
    {"id":"public_surface_wording_and_export","kind":"surface","oracle":"four surfaces plus CPU ABI declaration and derived DLL export population"},
    {"id":"real_model_budget_decode","kind":"real_model","gate":"SUPERSLM_G5_REAL_MODEL_TESTS","oracle":"budget stop, cpu=0,gpu=0,bound=1"},
    {"id":"real_model_late_bind","kind":"real_model","gate":"SUPERSLM_G5_REAL_MODEL_TESTS","oracle":"live and SLM5-restored late bind reject"}
  ],
  "mutants": {
    "a":"unbound accepting writes one","b":"unbound bound writes one",
    "c":"SLM5 restore reports unbound","d":"membership implemented as state-nonzero",
    "e":"accepting search always false","f":"dead-end overwrites walk",
    "g":"CPU prompt-only adoption leaves stale walk","h":"GPU caches membership",
    "i":"GPU rebind uses prior schema identity","j":"universal acceptance rule",
    "k":"GPU late-bind context-length guard removed","l":"GPU Idle reset layer guard restored"
  }
}
