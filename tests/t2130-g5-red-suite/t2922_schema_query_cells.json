{
  "plan": "Claude/Plans/te266-gpu-path.md#3.10.8",
  "plan_commit": "38420c8a9a",
  "engine_red_pin": "e99b09497e018847a9b59e8f9b5cc8d720ce6d00",
  "fixture": {
    "schemas": ["t2922_accepts_q", "t2922_rejects_q"],
    "same_reachable_state": "q",
    "acceptance": {"t2922_accepts_q": 1, "t2922_rejects_q": 0}
  },
  "cells": [
    {"id":"BIND_FRESH_RESET_RESTORED_RESET_ACCEPT","oracle":"CPU/GPU bind, rebind and unbind accept after create/reset; every restored form rejects unchanged until reset, then accepts"},
    {"id":"BIND_AFTER_PROMPT_PREFILL_REJECT","oracle":"CPU/GPU live and SLM5-restored prompt histories reject bind/rebind/unbind without changing the save blob"},
    {"id":"BIND_AFTER_DECODE_REJECT","oracle":"CPU/GPU normal decoded-token histories reject bind/rebind/unbind without changing the save blob"},
    {"id":"BIND_AFTER_DEAD_END_REJECT","oracle":"CPU/GPU TE-377 dead-end histories reject bind/rebind/unbind without changing the save blob"},
    {"id":"BIND_AFTER_REFUSED_FIRST_CALL_REJECT","oracle":"one real first-call prompt guard refusal on each backend rejects bind/rebind/unbind unchanged"},
    {"id":"RESET_REUSE","oracle":"CPU/GPU dead-end and ordinary throttled partial-token histories reset and then decode a real token"},
    {"id":"RESET_AFTER_GUARD_REFUSAL","oracle":"the representative first-call guard refusal resets and re-enters the caller on CPU/GPU, reproducing the guard rather than a reset/busy refusal"},
    {"id":"SCHEMA_QUERY_REAL_DECODE","oracle":"retained accepting/bound query state, status, parity and registered real-model budget cells"},
    {"id":"ADAPTER_SWAP_RESET","oracle":"a real mapped adapter swap rejects at an ordinary throttled partial token and the identical swap succeeds after reset on CPU/GPU"}
  ],
  "query_subcells": [
    "unbound_application_final", "bound_mid_walk", "bound_accepting",
    "dead_end_predecessor", "slm5_restore_bound", "slm4_restore_unbound",
    "reset_preserves_binding", "cpu_adopt_prefix_progress",
    "cpu_adopt_prefix_prompt_only", "submitted_busy_unchanged_out",
    "malformed_foreign_unchanged_out", "drain_then_finalize_query",
    "distinct_schema_restore_rebind", "no_device_work",
    "public_surface_wording_and_export", "real_model_budget_decode"
  ],
  "retired": [
    "BIND_ORIGIN_MATRIX.strict_layer", "BIND_ORIGIN_MATRIX.full_depth",
    "BIND_ORIGIN_MATRIX.zero_commit", "RESTORED_BIND_ORIGIN_MATRIX.synthetic_sources",
    "BIND_NO_WRITE_ACCEPT", "BIND_EMPTY_ADOPT_ACCEPT", "BIND_POST_SUBMIT_NO_TOKEN",
    "queue_allocation_inflight_publication_mutations", "first_write_tables",
    "generation_history_field_censuses", "delegated_callback_placement",
    "ExecuteCommandLists_placement_claims", "TE380_TE384_historical_probe_reruns",
    "inverse_exit_apparatus"
  ],
  "mutants": {
    "a":"unbound accepting writes one",
    "b":"unbound bound writes one",
    "c":"SLM5 restore reports unbound",
    "d":"membership implemented as state-nonzero",
    "e":"accepting search always false",
    "f":"dead-end overwrites walk",
    "g":"CPU prompt-only adoption leaves stale walk",
    "h":"GPU caches membership",
    "i":"GPU rebind uses prior schema identity",
    "j":"universal acceptance rule",
    "k":"D-SLM7625 bind flag deletion; exact future anchor prepared",
    "l":"one GPU prompt-prefill generation entry clear omitted; exact future anchor prepared",
    "m":"reintroduced GPU Idle reset refusal"
  }
}
