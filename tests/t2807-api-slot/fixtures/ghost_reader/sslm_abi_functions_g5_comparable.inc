/* T-2835 (Curie) -- the second half of the reader's must-reject fixture; see sslm_abi_functions.inc. */
#include "superslm/api.h"

SSLM_C_EXPORT sslm_status sslm_ghost_macro(sslm_model model, sslm_seq* out);
