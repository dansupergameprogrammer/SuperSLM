"""Loki's two red negative-control fixtures, reproduced from the strike casebooks so the P5
ablation-robustness and cross-race reversibility suites (Curie's build, out of this package's scope)
have a known-fail construction to test against before either control is trusted on the real cast
(`RuntimeLoRA_VoiceDemo_Plan.md` Sec.8 P1's own gate)."""
